/**
 * نام پیش‌نویس سفارش همین زبانه در sessionStorage (برش ۳د، ADR-036).
 *
 * جدا و بی وابستگی، چون سه جا لازمش دارند که هیچ‌کدام نباید بقیه را بیاورد: اسکریپت درون HTML صفحهٔ اصلی
 * (`app/page.tsx`، پیش از رسم)، خود پیش‌نویس (`lib/draft.ts`، تکهٔ رابط پس از فایل)، و صفحهٔ سفارش
 * پرداخت‌شده که پاکش می‌کند.
 */
export const DRAFT_KEY = 'jy.draft';

/**
 * نشانهٔ ریشهٔ صفحه تا جزوه برگردد: اسکریپت درون HTML اگر پیش‌نویسی هست می‌گذاردش، CSS با آن صفحه را از همان
 * اولین رسم در حالت سفارش نشان می‌دهد (`app/home.css`)، و پوستهٔ فلوی سفارش وقتی جزوه رسید یا نرسید برش می‌دارد.
 */
export const RESTORING_ATTR = 'data-restoring';
