# وابستگی‌های زمان اجرای کارگر اسناد — پین‌شده.
#
# سرور به PyPI دسترسی ندارد (مثل npm، ADR-019). CI از همین فایل چرخ‌های آماده
# می‌سازد و سرور فقط `pip install --no-index` از آنها می‌کند.
pymupdf==1.27.2.3
numpy==2.3.5
psycopg[binary]==3.2.13
