# وابستگی‌های زمان اجرای کارگر اسناد — پین‌شده.
#
# سرور به PyPI دسترسی ندارد (مثل npm، ADR-019). CI از همین فایل چرخ‌های آماده
# می‌سازد و داخل ایمیج پایه (Dockerfile.base، ADR-027) نصب می‌کند؛ عوض کردن این
# فایل یعنی ایمیج پایهٔ تازه و یک دانلود ۲۵۰ مگابایتی روی سرور.
pymupdf==1.27.2.3
numpy==2.3.5
psycopg[binary]==3.2.13
# عکس به PDF (برش ۲ب): PyMuPDF خودش WebP و HEIC را نمی‌خواند.
pillow==12.3.0
pillow-heif==1.8.0
