/**
 * متن فارسی و اعداد.
 *
 * دو کار جدا که به هم می‌رسند:
 *  - **نرمال‌سازی**: هر متنی که از کاربر یا از فایل پست می‌آید باید یکدست شود،
 *    وگرنه «ميردريكوندي» و «میردریکوندی» دو رشتهٔ متفاوت‌اند و تطبیق شکست می‌خورد.
 *  - **نمایش**: قیمت و تعداد با ارقام لاتین («147 صفحه»، «245,000 تومان»)،
 *    تاریخ شمسی. این تصمیم بریف است و در کل UI رعایت می‌شود.
 *
 * هر بخش ماژول خودش است و این فایل فقط همه را دوباره صادر می‌کند؛ بسته `sideEffects: false` است، پس
 * webpack هر بخش را فقط در تکه‌ای می‌گذارد که به کارش برده. صفحهٔ اصلی پیش از فایل فقط نرمال‌سازی نام
 * فایل را می‌خواهد (`normalize.ts`)؛ نمایش عدد و پول و تاریخ با رابط پس از فایل می‌آیند، نه با باندل اولیه
 * (برش ۳ج؛ پیش از آن همه در یک ماژول بودند و هر خروجیِ به‌کاررفته‌ای با باندل اولیه می‌آمد).
 *
 * نشانی، نام گیرنده و شمارهٔ موبایل (`tidyInputFa`، `normalizeIranMobile`) جدا در `@jozveyar/text/input`اند،
 * فقط برای مسیر خرید و سرور.
 */

export * from './normalize.js';
export * from './post.js';
export * from './format.js';
export * from './dates.js';
