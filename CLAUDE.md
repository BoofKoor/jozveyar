# جزوه‌یار — سفارش آنلاین چاپ جزوه

سایت سفارش چاپ و صحافی جزوه. بازار ایران، فارسی، RTL. دامنه: jozveyar.com

> **وضعیت (۱۴۰۵/۰۷/۰۱):** سایت روی `https://jozveyar.com` زنده است.
> برش ۰ و ۱ ساخته و مستقر شده‌اند. برش ۲الف تمام: پایگاه داده، آپلود chunked
> مستقیم به Garage، و کارگر تحلیل پایتون (`services/docworker`) با هم‌ترازی قیمت.
> برش ۲ب، دو قدم اول: ایمیج پایه با LibreOffice و فونت‌ها (ADR-027، مستقر)، و
> تبدیل Word و پاورپوینت و عکس به PDF روی سرور با پیش‌فاکتور فوری مرورگر (ADR-028).
>
> طرح کامل در `docs/ARCHITECTURE.md`، تصمیم‌ها در `docs/DECISIONS.md` (۲۸ ADR).
>
> **قدم بعدی:** برش ۲ب قدم ۳ — هشدارها (کیفیت اسکن، حاشیه، صفحهٔ خالی، فونت
> جایگزین‌شده)؛ بعد قدم ۴ — چند فایل در یک جزوه. یا اگر صاحب پروژه بخواهد، برش ۳
> (سفارش) زودتر.
>
> **استوریج:** Garage روی همان سرور، نه MinIO (ADR-023 — ایمیج MinIO از
> Docker Hub حذف شد). دیسک: ۸۷ از ۹۶ گیگابایت آزاد؛ سقف فایل‌ها ۴۰ گیگابایت.
> `deploy-bundle.sh` خودش `setup-storage.sh` را صدا می‌زند؛ کار دستی ندارد.
>
> **مقیاس: فعلاً یک سرور.** نود تحلیل یا استوریج جدا ساخته نمی‌شود، ولی درش
> بسته نیست — هر کد تازه باید همین را نگه دارد: کارگر بی‌حالت، صف در پستگرس
> (`SKIP LOCKED`)، استوریج پشت S3 با آدرس از `.env`، هیچ فرضی دربارهٔ
> «همه روی یک ماشین‌اند» (مسیر فایل محلی مشترک، localhost بین سرویس‌ها،
> قفل در حافظه). نود تحلیل دوم = همان ایمیج با همان `.env` (و اگر فقط تحلیل
> کند، `DOCWORKER_KINDS=analyze_document`)؛ استوریج جدا = `S3_ENDPOINT` دیگر.
> (ADR-023، ADR-025، ADR-028)
>
> **بیلد روی سرور انجام نمی‌شود.** گیت‌هاب اکشنز بسته را می‌سازد و سرور با
> `./infra/deploy-bundle.sh` یک فایل ۱۷ مگابایتی می‌گیرد. دلیل در ADR-019.
> ایمیج پایهٔ کارگر (LibreOffice، فونت‌ها، چرخ‌ها؛ ۲۸۸ مگابایت در ۸ تکه) جدا و فقط
> وقتی عوض شده می‌آید، با `docker load` (ADR-027). فونت خصوصی (سری B) در مخزن عمومی
> نمی‌رود: `./infra/upload-fonts.sh <پوشه>` آن را به پیشوند `fonts/` باکت می‌برد.

## تز محصول — تغییر نمی‌کند

فایل را در مرورگر بخوان، بعد قیمت را نشان بده. قبل از ثبت‌نام، قبل از هر تایپی.

- قیمت قبل از هویت. تا لحظهٔ پرداخت هیچ لاگینی نیست.
- صفر تایپ تا مرحلهٔ آدرس. شهر با یک تپ انتخاب می‌شود.
- تک‌ستونی، یک تصمیم در هر لحظه. قیمت همیشه روی صفحه؛ در موبایل نوار چسبان پایین.
- هیچ بن‌بستی نداریم. «به تلگرام پیام بدهید» شکست است، نه راه‌حل.
  مرورگر کم آورد → بی‌صدا مسیر سرور. کارگر کم آورد → پیام روشن + راه جلو.

## قواعد شکست‌ناپذیر

1. **قیمت را دو بار پیاده نکن.** موتور قیمت فقط در `packages/pricing` است و همان کد
   در مرورگر و سرور اجرا می‌شود. هر منطق قیمت جای دیگر، باگ است.
2. **سرور منبع حقیقت است.** قیمت مرورگر پیش‌فاکتور است. هیچ‌وقت به عدد کلاینت اعتماد نکن.
3. **پول: `bigint` و ریال.** نام هر متغیر پول به `Rials` یا `Tomans` ختم می‌شود.
   `price` خالی ممنوع. تبدیل فقط در لایهٔ نمایش و ورودی ادمین.
4. **رنگ همیشه per-page تحلیل و ذخیره می‌شود** حتی وقتی UI فقط انتخاب کلی می‌دهد.
   فایل بعد از ۲ روز پاک می‌شود؛ دادهٔ از دست رفته برنمی‌گردد.
   اعداد خام (`colorRatio`, `chromaP95`, `paperCast`) را ذخیره کن، نه فقط بولین.
5. **انتخاب رنگ = فهرست قاعده با بازهٔ صفحات**، نه فلگ روی سفارش و نه یک سطر به‌ازای هر صفحه.
   حالت فعلی (`features.per_page_color = false`) یعنی UI فقط یک قاعده می‌سازد.
6. **قیمت سفارش منجمد است.** `price_list_version` و `price_breakdown` ذخیره می‌شوند.
   قیمت یک سفارش موجود هیچ‌وقت بازمحاسبه نمی‌شود.
7. **سرویس بیرونی = آداپتور.** پرداخت، پیامک، استوریج و ارسال همه پشت اینترفیس.
   هیچ SDK بیرونی مستقیم در کد دامنه.
8. **هیچ منبع خارجی در زمان اجرا.** قلم، اسکریپت و CSS همه لوکال.
   مقصد نهایی هاست ایران است؛ این اجبار است نه سلیقه.

## زبان و نمایش

- کل UI راست‌چین. فقط logical properties (`margin-inline-start`)، هرگز `left/right`.
- قلم وزیرمتن، فقط وزن ۴۰۰ و ۶۰۰. لوکال و ساب‌ست‌شده.
- اعداد و قیمت با ارقام لاتین: «147 صفحه»، «245,000 تومان». تاریخ‌ها شمسی.
- ورودی کاربر همیشه فارسی‌نرمال می‌شود (`packages/text`): ي→ی، ك→ک، ارقام، نیم‌فاصله.

## پالت — فقط همین رنگ‌ها

| نقش | کد |
|---|---|
| سبز عمیق | `#6E7A5E` |
| سبز میانی | `#A8BC95` |
| سبز کم‌رنگ | `#D9E6C9` |
| سبز دکمه | `#D0E5B8` |
| پس‌زمینه چیپ | `#E4EEDC` |
| پس‌زمینه صفحه | `#F4F4F1` |
| کارت | `#FFFFFF` |
| خط مویی | `#E6E6E1` |
| متن اصلی | `#16181A` |
| متن ثانویه | `#6B6F70` |

رنگ خارج از این فهرست در کد ممنوع است. همه به‌صورت توکن در `packages/ui`.

## چیدمان

```
apps/web          Next.js — سایت عمومی، فلوی سفارش، API
apps/admin        Next.js — پنل ادمین (ساب‌دامین و کانتینر جدا)
services/docworker  Python — تبدیل Word، پاورپوینت و عکس (LibreOffice، Pillow) و تحلیل PDF (PyMuPDF)
packages/pricing    موتور قیمت — TS خالص، بدون وابستگی، مرورگر + سرور
packages/analysis   الگوریتم تشخیص رنگ و صفحه — مشترک با کارگر
packages/contracts  اسکیمای zod و تایپ‌های مشترک
packages/db         اسکیمای drizzle + مهاجرت‌ها
packages/storage    آداپتور استوریج S3 — SigV4 دست‌نویس، بدون SDK
packages/text       نرمال‌سازی فارسی، ارقام، تاریخ شمسی
packages/ui         کامپوننت و توکن‌های پالت
infra/              docker-compose، Caddyfile، اسکریپت دیپلوی
docs/               ARCHITECTURE.md، PRICING.md، DECISIONS.md
```

## واحدها — منبع اصلی اشتباه

- **صفحه** = یک رو. **برگ** = یک ورق = دو صفحه در حالت دورو.
- بازه‌های قیمت صحافی بر حسب **برگ** است. اشتباه گرفتن این دو قیمت را خراب می‌کند.
- بالای ۸۰۰ برگ خودکار چند جلد می‌شود (`binding_types.max_sheets_per_volume`).
- تعرفهٔ فعلی: چاپ رنگی ۲۰۰۰ و سیاه‌سفید ۱۶۰۰ تومان **به‌ازای هر رو**.

## ورود فایل پست — تله‌های اثبات‌شده

فایل پست جدول HTML با پسوند `.xls` است، نه اکسل. فرمت را از محتوا تشخیص بده.

- کد سفارش **فقط** از ستون «نام گ» و **فقط** با الگوی «عدد در انتهای رشته» خوانده می‌شود.
  هیچ‌وقت در همهٔ ستون‌ها نگرد: ستون وزن هم عدد ۴ رقمی دارد و تطبیق غلط می‌دهد.
- نام خانوادگی می‌تواند چندکلمه‌ای باشد («بیک زاده 6098») — روی فاصله نشکن.
- ردیف «جمع کل» را دور بریز: بارکدش عددی نیست.
- «شماره مرجع» و «شماره ثبت» سطح دسته‌اند، نه سفارش. برای تطبیق استفاده نکن.
- وزن و کرایهٔ واقعی را در `shipments` ذخیره کن — گزارش حاشیهٔ ارسال به آن تکیه دارد.
- سطر با اطمینان پایین هیچ‌وقت بدون تأیید ادمین پیامک نمی‌شود.

## دستورها

```
pnpm dev            سرور توسعه
pnpm build          بیلد همهٔ پکیج‌ها و اپ
pnpm test           تست واحد (قیمت، تحلیل، متن، تبدیل تعرفه)
pnpm test:e2e       تست سرتاسری مرورگر — نیازمند build
pnpm typecheck      بررسی تایپ همهٔ پکیج‌ها
pnpm check          typecheck + تست واحد
pnpm fixtures       ساخت PDF نمونه برای تست

docker compose -f infra/docker-compose.yml up     پستگرس، Redis، Garage لوکال
pnpm --filter @jozveyar/db migrate               اعمال مهاجرت‌ها (نیازمند DATABASE_URL)
DEPLOY_HOST=user@ip ./infra/deploy.sh             استقرار روی VPS

cd services/docworker && python -m pytest -q tests   تست کارگر اسناد (پایتون ۳.۱۲؛ تست‌های LibreOffice
                                                    بیرون ایمیج پایه رد می‌شوند — CI داخلش اجرا می‌کند)
python -m docworker                                 اجرای کارگر (همان env اپ)
./services/docworker/base-id.sh                     شناسهٔ ایمیج پایهٔ کارگر (هش ورودی‌هایش، ADR-027)
./infra/upload-fonts.sh <پوشه>                      فونت خصوصی به باکت، روی سرور (ADR-027)
UPDATE_PARITY=1 pnpm test                           بازنویسی بردارهای هم‌ارزی بعد از تغییر عمدی الگوریتم رنگ
```

**LibreOffice فایل ناشناس باز می‌کند.** فقط از `convert.LibreOffice` اجرایش کن
(پروفایل مهارشده، محیط تمیز، `sandbox.run_confined`) و `init: true` را به compose
کارگر برنگردان: کارگر خودش PID 1 و non-dumpable است تا LibreOffice آلوده رمزها را
از `/proc` نخواند. هر دیوار تستی با «شاهد» دارد (ADR-028).

**الگوریتم رنگ دو پیاده‌سازی دارد** — `packages/analysis` (مرورگر) و
`services/docworker/docworker/analysis.py` (سرور). هر تغییری در یکی باید در
دیگری هم بیاید؛ `packages/analysis/parity/vectors.json` برابری **دقیق** را در
هر دو تست قفل کرده (ADR-025).

تست سرتاسری روی build تولیدی اجرا می‌شود، نه dev server: چیزی که می‌سنجد
(اندازهٔ باندل اولیه و تنبل بودن pdf.js) فقط در build تولیدی معنا دارد.

تست‌های یکپارچگی پایگاه داده بدون `DATABASE_URL` خودشان را رد می‌کنند. با آن،
مهاجرت‌ها روی پستگرس واقعی اجرا می‌شوند و محدودیت‌های تعرفه سنجیده می‌شوند:

```
DATABASE_URL=postgresql://jozveyar:jozveyar@127.0.0.1:5432/jozveyar pnpm test
```

تست‌های استوریج هم همین‌طور، با `S3_ENDPOINT` (و `S3_ACCESS_KEY`،
`S3_SECRET_KEY`، `S3_BUCKET`) روی یک Garage واقعی. CI هر دو را همیشه بالا
می‌آورد، روی PR هم. تست سرتاسری آپلود واقعی (`tests/upload.spec.ts`) با
`E2E_UPLOAD_BASE_URL` اجرا می‌شود؛ طرز اجرا بالای همان فایل است.

## اعدادی که اندازه گرفته شده‌اند

اینها در تست قفل‌اند؛ اگر پس‌رفت کنند تست می‌شکند:

- اولین قیمت برای اسکن زرد ۱۴۷ صفحه‌ای، با پردازندهٔ ۴ برابر کند: **۴۸۶ms**
  (سقف پذیرش: ۲ ثانیه)
- باندل اولیهٔ صفحهٔ اصلی: **۱۳۹ کیلوبایت** — pdf.js، آپلودگر و خوانندهٔ فهرست zip
  (پیش‌فاکتور Word) داخلش نیستند (۱۳۶ بود؛ دو کیلوبایت برای آپلود و هم‌ترازی با
  قیمت سرور، یکی برای مسیر Word و عکس)
- همان اسکن زرد روی **سرور**: همهٔ ۱۴۷ صفحه در **۱٫۴ ثانیه**، ۰ صفحهٔ رنگی
  (سقف تست ۱۵ ثانیه)
- همان اسکن زرد: **۰ صفحهٔ رنگی** تشخیص داده می‌شود، قیمت ۲۸۰,۲۰۰ تومان
  (اگر تشخیص رنگ بشکند ۳۳۹,۰۰۰ می‌شود)
- Word فارسی ۱۲۰ پاراگرافی با «B Nazanin» (Nazli جایش می‌نشیند): **۱۱ صفحه** با
  LibreOffice ۲۵.۲.۳ ایمیج، تبدیل در **۰٫۹ ثانیه** (`PERSIAN_WORD_PAGES` در
  `tests/test_convert.py`)

## پروکسی و TLS

Nginx، نه Caddy. گواهی با certbot دستی صادر و با cron تمدید می‌شود
(`infra/setup-tls.sh`). اگر صدور از داخل ایران گیر کرد، گواهی را از جای دیگری
بگیرید و در `infra/certs/live/<دامنه>/` بگذارید — Nginx تفاوتی نمی‌بیند.

## مهاجرت

Drizzle. مهاجرتِ اعمال‌شده هیچ‌وقت دستی ویرایش نمی‌شود — مهاجرت جدید بساز.
بازه‌های تعرفه با محدودیت `EXCLUDE` محافظت می‌شوند؛ دستکاری نکن.

## قبل از هر تغییر

تصمیم‌های معماری در `docs/DECISIONS.md` است. اگر تغییری با آن در تناقض است، اول بپرس.
صاحب پروژه با «برنامه اول، تأیید، بعد اجرا» کار می‌کند.
